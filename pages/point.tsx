import { useLayout } from "../hooks";

const PointShop = () => {
    return (
        <div>
            this is Point Shop
        </div>
    );
}

PointShop.getLayout = useLayout()

export default PointShop;